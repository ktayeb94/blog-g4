
<%= simple_format comment.text %>
Published by <%= link_to comment.user.blog_name, articles_path(comment.user.blog_name)%>
<%= link_to 'Destroy', article_comment_path(comment.article.user.blog_name, comment.article, comment), method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Report', report_article_comment_path(comment.article.user.blog_name,   comment.article, comment), :method => :post %>
<% if comment.report_count > 0%>
  (Nb of reports : <%= comment.report_count %> )
<% end %>

